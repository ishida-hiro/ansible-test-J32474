---
# 設計書 1.8「時刻同期設定」／旧シート2「13.NTP設定」
#   ※現行のシート8（ファイルサーバ）に NTP 設定の節は無い＝ドメインから自動同期
#   ・操作マスタ(FSMO)の PDC を保持する DC が外部 NTP と手動同期する
#   ・ドメイン参加サーバは自動でドメインコントローラと同期するため設定不要
is_pdc_emulator: false
ntp_servers: "ntp.nict.jp,0x8"
